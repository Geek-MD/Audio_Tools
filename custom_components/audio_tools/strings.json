{
  "config": {
    "step": {
      "user": {
        "title": "Audio Tools — Setup",
        "description": "Configure the Audio Tools integration. The **Work Directory** is optional — if provided it is shown in the UI for reference, but each service call always specifies the full file path explicitly.",
        "data": {
          "work_dir": "Work Directory (optional)"
        }
      }
    },
    "error": {
      "unknown": "Unexpected error. Please check the logs for more information."
    },
    "abort": {
      "already_configured": "Audio Tools is already configured."
    }
  }
}
